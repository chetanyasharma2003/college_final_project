{
  "buildCommand": "npm run build",
  "framework": "vite",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "^/(?!api).*$",
      "dest": "/index.html",
      "status": 200,
      "headers": {
        "Cache-Control": "public, max-age=0, must-revalidate"
      }
    }
  ]
}
